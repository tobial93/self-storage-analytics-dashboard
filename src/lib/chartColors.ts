/** Shared chart color palette — use these everywhere so the same dimension always gets the same color. */
export const CHART_COLORS = ['#00d4aa', '#ff6b9d', '#f5a623', '#00a3cc', '#a78bfa']

/** Revenue is always teal across all charts. */
export const COLOR_REVENUE = '#00d4aa'

/** Ad Spend is always pink across all charts. */
export const COLOR_SPEND = '#ff6b9d'
